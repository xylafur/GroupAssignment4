#ifndef UNSORTED_ARRAY
    #define USORTED_ARRAY
#include <stdio.h>
#include <iostream>

template <class T>
class UnsortedArray{
private:
    T * arr;
public:
    UnsortedArray(int size){
        
    }
};

#endif
